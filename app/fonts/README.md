# Fuentes locales

## Blocks (display / titulares)

TODO: Coloca aquí el archivo de la fuente **Blocks** (idealmente `Blocks.woff2`).

Luego abre `app/fonts.ts` y sigue las instrucciones del comentario para activarla
(descomentar el bloque `localFont` y reemplazar el placeholder `Archivo Black`).

Formatos recomendados, en orden de preferencia: `.woff2` > `.woff` > `.ttf`/`.otf`.
