# Videos

TODO: Coloca aquí tus videos reales.

- `showreel.mp4` / `showreel.webm` → fondo del Hero (`components/sections/Hero.tsx`).
- Clips de preview del portafolio → referenciados en `lib/content.ts` (`PORTFOLIO[].video`).

Recomendación: comprime los videos (HandBrake / ffmpeg), mantenlos mudos
(`muted` para autoplay) y ofrece `.webm` + `.mp4` de respaldo.
